name: aur-push

on:
  push:
    paths:
      - "*/PKGBUILD"
  workflow_dispatch


jobs:
  srcinfo:
    name: srcinfo
    runs-on: ubuntu-latest
    steps:
      - uses: enkodin/action-archlinux@v1
      - name: Test
        run: pwd
